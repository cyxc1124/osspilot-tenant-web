# tenant-api base URL for local Vite dev (no trailing slash).
# Leave empty to use the Vite /api proxy (default target http://localhost:8000).
# Docker / K8s: set OSSPILOT_API_URL at container start (see README.md)
VITE_TENANT_API_URL=http://localhost:8000
